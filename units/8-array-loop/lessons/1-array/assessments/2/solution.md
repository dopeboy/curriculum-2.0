Create an array with four elements. Print out the last element of the array.

```
var teams = ["lakers", "nets", "raptors", "bobcats"];
console.log(teams[3]) OR console.log(teams[teams.length-1]);
```
